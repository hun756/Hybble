#include <gtest/gtest.h>

#include "array.hpp"
#include <vector>

TEST(ArrayTest, ArrayElement) {
    // Array<int, 3> arr{1, 2, 3};
    
    // ASSERT_EQ(arr.size(), 3);
    // ASSERT_EQ(arr[0], 1);
    // ASSERT_EQ(arr[1], 2);
    // ASSERT_EQ(arr[2], 3);
}